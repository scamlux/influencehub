# Frontend env — only these are read by the app (all VITE_-prefixed).
#
# Leave BOTH blank to run entirely on the built-in mock data layer
# (no backend required). Fill them in to connect a real Supabase project.
VITE_SUPABASE_URL=https://ceqpcuhguxjdbzuuwamr.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNlcXBjdWhndXhqZGJ6dXV3YW1yIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE5MjI2NjIsImV4cCI6MjA5NzQ5ODY2Mn0.mM57BY5csrf67uY9E9X4ft6jKxpf3WKH57r6HLwnoas

# Optional — enables real Stripe Checkout in the browser. Safe to omit.
# VITE_STRIPE_PUBLISHABLE_KEY=

# Optional — real YouTube stats during influencer onboarding. Restrict this key
# by HTTP referrer in Google Cloud (it ships in the browser bundle).
# VITE_YOUTUBE_API_KEY=

# Optional override — force mock mode even when the URL/key above are set.
# VITE_USE_MOCK_DATA=true

# Optional — frontend error reporting (T-03). Leave blank to disable Sentry.
# VITE_SENTRY_DSN=
